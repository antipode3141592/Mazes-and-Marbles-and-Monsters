using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Board : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //every frame, check X and Y inputs to adjust accelation on all active player type objects (player, monster, marble)
        // each object reacts differently to the forces (player and monsters have more inertia and/or friction than the marbles)


    }
}
